# Supabase Configuration — same project as the Streamlit admin portal.
# These are also baked into lib/config/supabase_config.dart as defaults so the
# app runs without --dart-define. Override at build time with:
#   flutter run --dart-define=SUPABASE_URL=... --dart-define=SUPABASE_ANON_KEY=...
SUPABASE_URL=https://hpooyhqbooruuvhziwei.supabase.co
SUPABASE_ANON_KEY=sb_publishable_4SyX2VTIPXR-T2DQhh4x0g_r1-K0OrT
SUPABASE_SERVICE_KEY=

# FastAPI Backend (face recognition + check-in)
API_BASE_URL=http://10.79.79.4:8000
API_TIMEOUT_SECONDS=30

# Face Recognition
MIN_FACE_CONFIDENCE=0.7
FACE_FRAME_SIZE=100

# Geofence
DEFAULT_GEOFENCE_RADIUS=100.0

# Environment
ENVIRONMENT=development
DEBUG_MODE=true
