# Supabase connection for the Lumenor HRMS web app.
# The anon/publishable key is safe to expose in a browser bundle.
# Copy to `.env.local` for local dev, and add the same vars in the Vercel
# project settings (Environment Variables) before deploying.
VITE_SUPABASE_URL=https://hpooyhqbooruuvhziwei.supabase.co
VITE_SUPABASE_ANON_KEY=sb_publishable_4SyX2VTIPXR-T2DQhh4x0g_r1-K0OrT
